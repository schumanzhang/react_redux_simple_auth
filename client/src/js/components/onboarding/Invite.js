import React from "react";
import { connect } from 'react-redux';
import { push } from 'react-router-redux';


export default class Invite extends React.Component {

    render(){  
        return (
         <div>
           <h1>Now we invite clients to the app</h1>
         </div>
        );
    }

}